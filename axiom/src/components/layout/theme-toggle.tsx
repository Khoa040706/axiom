/**
 * theme-toggle.tsx — Deprecated
 * Toggle dark/light mode đã được tích hợp trực tiếp vào (dashboard)/layout.tsx
 * và persistent qua dashboard-context.tsx (localStorage).
 * File này giữ lại để tham khảo, không được import hay sử dụng.
 */
export {}
