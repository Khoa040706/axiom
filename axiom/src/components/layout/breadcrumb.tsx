/**
 * breadcrumb.tsx — Deprecated
 * Chức năng này đã được tích hợp trực tiếp vào (dashboard)/layout.tsx
 * File này giữ lại để tham khảo, không được import hay sử dụng.
 *
 * Nếu cần breadcrumb riêng, implement tại đây và import vào layout.tsx
 */
export {}
