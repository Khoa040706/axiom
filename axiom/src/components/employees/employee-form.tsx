/**
 * @deprecated
 * Component này đã được thay thế bởi inline modal trong
 * `src/app/(dashboard)/employees/page.tsx` (kết nối DB thật,
 * dropdown phòng ban/chức vụ từ server action).
 *
 * File giữ lại để tham khảo cấu trúc form nếu cần tách lại
 * thành component riêng trong tương lai.
 *
 * TODO (P3): Nếu cần refactor lại, tạo <EmployeeFormModal />
 * riêng nhận props: departments, positions, initialData, onSave, onCancel
 * và sử dụng từ page.tsx thay vì inline JSX.
 */

export {}
